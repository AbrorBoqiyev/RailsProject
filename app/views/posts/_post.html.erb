<div id="<%= dom_id post %>">
  <h4>posted bu <%= post.user.email %> </h4>
  <h3><%= post.title %></h3>
  <h6 style="color: lightgreen"><%= pluralize(post.views, "view") %></h6>
  <div>
    <%= post.body %>
  </div>

</div>
