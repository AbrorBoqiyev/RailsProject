<%= form_with(model: [post, comment]) do |f| %>
    <div class="form-control">
        <%= f.rich_text_area :body %>
        <%= f.submit submit_label, class: "btn btn-outline-primary m-1" %>
    </div>
<% end %>